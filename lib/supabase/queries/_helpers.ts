// File removed — supabaseTable workaround no longer needed after fixing the
// Database type structural compatibility with @supabase/supabase-js v2.45+
// GenericSchema. See lib/auth/require-admin.ts and lib/auth/require-customer.ts
// for the root fix (SupabaseClient<any, 'public', any>).
export {};
